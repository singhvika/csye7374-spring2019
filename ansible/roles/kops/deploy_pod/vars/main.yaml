---
  accountId: "{{ accountId }}"
  tag: "{{ tag }}"
  appName: "{{ appName }}"
  ecr: "{{ ecr }}"
  containerName: "{{ containerName }}"
  appName: "{{ appName }}"
  podName: "{{ podName }}"
  s3endpoint: "{{ s3endpoint }}"
...